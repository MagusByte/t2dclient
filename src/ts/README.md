# Todo2d.com Typescript client

The Todo2d typescript client contains everything to write your own automation.

First things first:
1. Not all calls are allowed. Certain calls will require a higher level of access.
2. The origin of the call is inspected, so this library can't be used in the frontend
